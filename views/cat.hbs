<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="style/output.css" rel="stylesheet">
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
<div class="bg-white shadow-lg rounded-lg p-8 w-full max-w-lg">
    <h2 class="text-3xl font-bold mb-6 text-gray-800">Информация о коте</h2>
    <div class="space-y-6">
        <div class="flex items-center space-x-6">
            <img src="{{data.image}}" alt="Кот" class="w-32 h-32 rounded-full object-cover shadow-md">
            <div>
                <h3 class="text-2xl font-bold text-gray-800">{{data.name}}</h3>
                <p class="text-gray-600"><span class="font-semibold">Порода:</span> {{data.breed}}</p>
                <p class="text-gray-600"><span class="font-semibold">Возраст:</span> {{data.age}} года</p>
                <p class="text-gray-600"><span class="font-semibold">Пол:</span> {{data.sex}}</p>
            </div>
        </div>
        <div>
            <p class="text-gray-700 leading-relaxed">
                {{data.description}}
            </p>
        </div>
        <div class="flex justify-between items-center">
            <a href="/" class="mt-4 bg-gray-300 text-gray-700 px-4 py-2 rounded-md hover:bg-gray-400 focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2 transition duration-300 ease-in-out">
                Вернуться
            </a>
            {{#if data.isHome}}
                <p class="mt-4 text-center bg-gray-500 text-white py-2 px-4 rounded-lg">Уже дома</p>
            {{else}}
                <a href="/reqAdminAdd?reqIndex={{data.id}}" class="block mt-4 text-center bg-blue-600 text-white py-2 px-4 rounded-lg hover:bg-blue-700 transition duration-300 ease-in-out">
                Хочу забрать
                </a>
            {{/if}}
        </div>
    </div>
</div>
</body>
</html>